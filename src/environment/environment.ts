export const environment = {
  production: false,
  // api: 'http://127.0.0.1:8000/api' // 🔹 Corrección: La clave debe ser `api`, no `apiUrl`
  api: 'http://localhost:3000/', // 🔹 Corrección: La clave debe ser `api`, no `apiUrl`
};
